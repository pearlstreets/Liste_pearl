// Authentication is handled by the Pearl Streets API
// No passwords stored in source code — all auth goes through the backend
export const MARKETPLACE_ACCOUNTS = [];
export const DEFAULT_ACCOUNT = null;
